/From the desk of Faheem Patel
!!!
%html

  %head
    %title Exam Countdown (G400)
    %link(rel="stylesheet" href="css/style.css")
    %link(rel="stylesheet" href="css/dyslexic.css")
    %meta(http-equiv="refresh" content="1800")

  %body
    %h2{:id => "title"} Exam Countdown

    - count = 0
    - @to_display.each do |exam|

      - if count == 0

        - if exam[1] < 0
          - next

        - attribute = "next"
        - count += 1

      - else 
        - attribute = "coming-up"

      %div{:class => attribute}
        %h3 
          = "#{exam[0]}"

        %p
          - if exam[0] == "Object-Oriented Programming"
            = "#{exam[2]}"
            %br
            = "#{exam[3]}, ITL"
          - else
            = "#{exam[2]}"
            %br
            = "#{exam[3]}, FB 326"

        - if exam[1] == 0
          %h1 TODAY
          %p{:class => "message"} Hey Awesome, yeah YOU, go kick some exam ass today.
          
        - elsif exam[1] == 1
          %h1 TOMORROW
          %p{:class => "message"} Be sure to sleep well.

        - else
          %h1
            = "#{exam[1]}"
            %br
            DAYS

