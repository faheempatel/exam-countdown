/From the desk of Faheem Patel
!!!
%html

  %head
    %title Exam Countdown (G400)
    %link(rel="stylesheet" href="css/style.css")
    %link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Quicksand:400,700")
    %link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Josefin+Sans:600,700")
    %meta(http-equiv="refresh" content="1800")

  %body
    %div{:id => "box"}
      %h2{:id => "title"} Exam Countdown

      - count = 0
      - @to_display.each do |exam|

        - if count == 0

          - if exam[1] < 0
            - next

          - class_name = "next"
          - count += 1

        - else
          - class_name = "coming-up"

        %div{:class => class_name}
          %h3 
            = "#{exam[0]}"
            
          %p
            = "#{exam[2]}"
            %br
            = "#{exam[3]}, "

            - if exam[5]
              %a{:href => exam[5]}
                = "#{exam[4]}"
            - else
              = "#{exam[4]}"

          - if exam[1] == 0
            %h1 TODAY
            
          - elsif exam[1] == 1
            %h1 TOMORROW

          - else
            %h1
              = "#{exam[1]}"
              %br
              DAYS
      %p{:style => "color: #292929; margin-bottom: 50px; text-align: left"}
        Thanks for using exam countdown, I hope it was useful. 
        %br
        %br
        If you're working on anything interesting over the Summer and have room for one more, I'd love to hear from you. You should shoot me an email  
        <a href = "mailto:hello@faheempatel.com?Subject=Summer project"/>here.</a>
        %br
        %br
        Thanks again,
        %br 
        Faheem
        %br

    %p{:id => "footer"} 
      exam countdown is a thing made by your friend
      %a{:href => 'http://faheempatel.com'} Faheem
