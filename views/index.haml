/From the desk of Faheem Patel
!!!
%html

  %head
    %title Exam Countdown (G400)
    %link(rel="stylesheet" href="css/style.css")
    %link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Quicksand:400,700")
    %link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Josefin+Sans:600,700")
    %meta(http-equiv="refresh" content="1800")

  %body
    %div{:id => "box"}
      %h2{:id => "title"} Exam Countdown

      - count = 0
      - @to_display.each do |exam|

        - if count == 0

          - if exam[1] < 0
            - next

          - class_name = "next"
          - count += 1

        - else
          - class_name = "coming-up"

        %div{:class => class_name}
          %h3 
            = "#{exam[0]}"
            
          %p
            = "#{exam[2]}"
            %br
            = "#{exam[3]}, "

            - if exam[5]
              %a{:href => exam[5]}
                = "#{exam[4]}"
            - else
              = "#{exam[4]}"

          - if exam[1] == 0
            %h1 TODAY
            %p{:class => "message", :style => "text-align: center;"} 
              Does that mean in Russia exam scared of you?
              %br
              Quick! Shh! There's no time to think. Grab your passport.

          - elsif exam[1] == 1
            %h1 TOMORROW
            %p{:class => "message"} 
              Legend has it if you high-five Faheem right before an exam all your dreams come true. High-five him after one at your own peril... hugs <b>(PLURAL!)</b> are fine though and are known to cure shyness.

          - else
            %h1
              = "#{exam[1]}"
              %br
              DAYS
    %p{:id => "footer"} 
      exam countdown is a thing made by your friend
      %a{:href => 'http://faheempatel.com'} Faheem
